# Inert library module. Core is not an installed Django app; do not list
# 'trusts' in INSTALLED_APPS. This file defines no Model subclasses and
# does not forward, lazy-export, or fallback-import Zero. Canonical
# models live at trusts.zero.models. Stored identity (app label trusts,
# migration keys, tables, content types, permissions, rows) is unchanged
# and Zero-owned.
