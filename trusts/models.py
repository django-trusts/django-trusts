# Inert library module. Django may import {name}.models when 'trusts' is
# listed. This file defines no Model subclasses and does not forward,
# lazy-export, or fallback-import Zero. Canonical models live at
# trusts.zero.models. Stored identity (app label trusts, migration keys,
# tables, content types, permissions, rows) is unchanged and Zero-owned.
