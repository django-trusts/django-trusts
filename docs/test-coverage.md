# Authorization test coverage

Recorded with the #14/#15 modernization. Existing tests were kept and
extended. This is not a claim that the declarative permission model is
complete.

This file is a chronological coverage record, not a public API reference.
Names described inside an older issue entry may have been removed later. Use
the current [README](../README.md), RST guide, and migration record for the
supported 1.x surface.

## Tests that must keep passing

Denied access and organization isolation (a trust is the organization
boundary):

- `TrustContentTestMixin.test_user_not_in_group_has_no_perm`
- `TrustContentTestMixin.test_has_perm_disallow_no_perm_content`
- `TrustContentTestMixin.test_has_perm_disallow_no_perm_perm`
- `TrustContentTestMixin.test_mixed_trust_queryset`
- `TrustContentTestMixin.test_denied_access_without_grant` (added)
- `TrustContentTestMixin.test_organization_isolation_denies_cross_trust_access` (added)
- `TrustTest.test_own_condition_requires_settlor` (added; `:own` is settlor-only)
- Role tests that a stronger role on one trust does not leak to another

These run for `Content` subclasses, `Junction` content, and `Trust` as content
where the mixin applies.

## Known expected failure

`TrustJunctionTestCase.test_read_permissions_added` remains
`@unittest.expectedFailure`. Junctions do not add `read_*` permissions on the
joined model. That is historical, not a new denial-widening.

## Coverage gaps (not closed here)

| Area | Why it is a gap |
| --- | --- |
| Anonymous request users | Settlor-anonymous is rejected; object-level anonymous grants are not specified. |
| Superuser short-circuit | Django's `ModelBackend` still treats superusers as having all perms; no extra Trusts test. |
| Nested / inherited trusts | `Trust.trust` is a parent pointer with a readonly check; no recursive ACL evaluation (out of scope). |
| `has_perm` without `obj` | Trusts contributes false/empty; ModelBackend listed separately restores globals (issue #77). |
| `get_group_permissions` content path | Declared terminals return the group-only common string set (issue #77). |
| Decorator `raise_exception=True` | Most decorator tests use `raise_exception=False`. |
| `TRUSTS_ENTITY_MODEL` swap | Custom entity model is configurable but untested. |
| Captured production MySQL/Postgres dump | Core retains `scripts/verify-legacy-upgrade.py` and `scripts/legacy/trusts_0001_sqlite.sql` as historical already-applied-`0001` evidence. That runner is currently broken/off CI and is not a live operator path. Zero has complementary command / fresh-install / grandfather tests, not an equivalent already-applied-`0001` replay. Not a customer dump and not a Django 1.8 contrib-schema upgrade. |
| Admin / i18n surfaces | Core models registered; `auto_modeladmin` opt-in is exercised in `AutoModelAdminTest`. |

## Issue #8 recovery tests (do not close #8)

These must keep passing in addition to the table above:

- `PermittedQuerySetTest` — trustee / TrustGroup local/global intersection / role-as-ceiling list-direct parity, SQL filter, inactive empty, `get_permission`, grant/revoke, arbitrary conditioned names raise `PermissionConditionNotQueryable`; unregistered `:own` on Category raises `AttributeError`
- `FilterByUserContentPermTest` — create-under-trust, no settlor shortcut, no parent-trust leak, inactive empty, `test_filter_by_user_perm` still discovered, conditioned names raise
- `AuthorizationTest` — reader/member denial with no mutation, scoped entity IDs, shared-group ceiling vs local grants, shared-group membership requires admin on every trust
- `TeamViewAuthorizationTest` — member GET/POST 403, admin add, unknown user PK does not mutate
- `AutoModelAdminTest` — Content and Junction proxies with `auto_modeladmin = True` register; opt-out does not
- `TrustGroupIntersectionTest` — issue #23 acceptance: no TrustGroup / empty local / local-without-ceiling deny; both layers allow; per-trust local subsets; removing either layer revokes; two groups combine without widening; trustee unchanged; role-derived ceiling; inactive/anonymous deny; `has_perm` / `.permitted` / `filter_by_user_content_perm` parity; legacy association grants nothing; grandfather dry-run/apply; later ceiling adds stay local-off; configured group/permission models
- `ConditionGrammarTest` — issue #4 / #142 Stage B: private `_ir` nodes (`==`/`!=`/`&`/`|`/refs/constants), nested grouping, builder-once proofs, chained comparisons and Python `or` raise `PermissionConditionBooleanError` at construction, calls/indexing/arithmetic/setters/`TQ` lookups rejected, prebuilt `Expr` / non-callable rejected at register with `TypeError`
- `ConditionRegistryIsolationTest` / `ConditionRegistryShapeTest` / `ConditionRegistryRuntimeTest` / `ConditionRegistryCheckTest` — issue #16 / #142 Stage B: per-handle IR-only records; builder invoked once with symbolic refs and never during evaluate/compile; freeze-before-invoke; callable-only `BackendHandle.register_permission_condition`; object/queryset parity and single-query list path; `assertNumQueries(0)` on Core registration and real `Ticket.meta_own` Meta donation plus idempotent re-entry; durable constant normalization; forbidden captures rejected; obsolete setting is `trusts.E007` and does not enable callbacks; public `Expr` / `condition_refs` / registry-store imports fail
- `PermissionConditionCheckLifecycleTest` / `ManagePyCheckLifecycleTest` — issue #29 / #142 Stage B: prebuilt `Expr` is `TypeError` before mutation; stuffed invalid IR still aggregates as `trusts.E001`; builders fail at register for exceptions / non-predicates / unresolved fields; `manage.py check` is the entrypoint; `TRUSTS_ALLOW_LEGACY_PERMISSION_CALLBACKS=True` is `trusts.E007` and does not restore callbacks; checks never re-invoke builders; `AppConfig.ready()` does not raise
- `StageBPublicSurfaceTest` / `StageBRegisterCallableOnlyTest` — issue #142 Stage B: `trusts.conditions` / `django_trusts` keep only the six accepted exceptions and permission-code helpers; former public construction and registry/compiler imports raise `ImportError`; private `_ir` remains for compiler tests; registering a prebuilt tree does not mutate the store
- `TrustsRegistryTest` — issue #57: isolated `TrustsRegistry` / root-relative `Ref` registration only (ordinary Document / DocumentGrant; inferred terminals; mixed-root / missing / scalar / multi-valued / reverse / GFK / non-model / `root` and `path` field-name refs / duplicate / conflict; zero queries). Not an authorization test
- `TrustsRegistryProjectionTest` — issue #60: isolated common plan and three projections (ordinary Document / DocumentGrant / DocumentPermit; enumeration match/non-match; object auth agrees with membership via SQL `EXISTS`; authorized-content agrees with object checks as one queryset; shared-row correlation; two roots OR; duplicate rows distinct; unregistered fail-closed; lazy filter / no per-object loop; configured and custom user terminals; no historical Trust/Content/Junction). Not a historical authorization test
- `CategoryPermittedRegistryTest` / `TrustsRegistryOwnershipTest` / `ContributorIdempotenceTest` — issue #67 Child H: package-owned registry on `AppConfig` (created in `__init__`, not replaced by `ready()`); explicit test-app TUP→Trust←Category contribution with #62 r3 idempotence (re-enter no-op; different-root Category still registers TUP; conflict fails closed without sentinel; new AppConfig/registry receives the declaration again); Alice sibling Categories on Trust A; Bob / wrong-perm deny; Carol group-only not erased; `has_perm` (old) ↔ `.permitted` (new) parity; lazy / one-query / `[:n]`; no-TUP deny with registration still present; named `:condition` constrains trustee|group; Category / Trust / Ticket skip `trust_grant_q` (Ticket is #72); Junction stays on the old path; reader uses `content_exists` not `filter_authorized`. No schema or migration.
- `TrustContributionIdempotenceTest` / `IsolatedAppsDoesNotDonateTrustContributionTest` / `TrustPermittedRegistryTest` — issue #70 S1: Trusts `AppConfig.ready()` contributes TUP→parent Trust←child Trust via `_meta` (no hard-coded reverse, no `_contents`); #62 r3 sentinel on this contribution (re-enter no-op; different-root Trust still registers TUP; conflict fails closed without sentinel; new AppConfig/registry and swapped live registry receive the declaration again; `ready()` does not replace the registry; `isolate_apps` without `trusts` does not touch the live Trust row); Alice sibling child Trusts on parent A not B; Bob / wrong-perm deny; Carol group-only not erased; `Trust:own` overlay including no underlying grant; mixed-parent isolation; `has_perm` (old) ↔ `.permitted` (new) parity; lazy / one-query / `[:n]`; create-under-trust stays on `trust_grant_q` (direct Trust terminal not registered); Junction stays on the old path (Ticket is #72). No schema or migration.
- `TicketContributionIdempotenceTest` / `IsolatedAppsDoesNotDonateTicketContributionTest` / `TicketPermittedRegistryTest` — issue #72 S2: test-app `TestsConfig.ready()` contributes TUP→Trust←Ticket via `_meta` (no hard-coded reverse, no `_contents`); Ticket has its own sentinel and is not completed by Category's; #62 r3 identity (re-enter no-op; Category sentinel does not skip Ticket; different-root Ticket still registers TUP; conflict fails closed without setting Ticket's sentinel; new AppConfig/registry and swapped live registry receive the declaration again; `isolate_apps` without `trusts` donates neither Category nor Ticket); Alice sibling Tickets on Trust A not B; Bob / wrong-perm deny; Carol group-only not erased; `:meta_own` overlay including no underlying grant; mixed-Trust isolation; `has_perm` (old) ↔ `.permitted` (new) parity; lazy / one-query / `[:n]`; create-under-trust stays on `trust_grant_q`; Junction stays on the old path. No schema or migration.
- `PathScopedRegistryStoreTest` / `ContributionPathTest` / `CompilerProtocolTest` / `TwoPathAuthorizationTest` / `CompilerIsolationTest` — issue #75 S3a: path-scoped `AppConfig.registries[path]` (one-path `registry` alias; duplicate-path de-dupe; same-class alias ambiguity; unconfigured path; zero/multiple alias fail; `ready()` does not replace the store; swapped/new registry identity; `isolate_apps`); explicit-path contribution, omitted ambiguous path fails before writing, no mixin fan-out, mixin-only gets no package Trust-as-content, concrete subclass does, contributor re-entry; `trusts.E004` plus runtime `TrustsCompilerError` (silencing does not enable fallback; a broken second path is not omitted); one-path Category/Ticket/Trust results unchanged; two paths same terminal (grant only A / only B / neither; aggregate OR of complete proofs in 1 SQL; failed ceiling cannot use the other path's fragment); different terminals route only through applicable handles; concrete historical TrustGroup vs mixin-only plan compiler isolation (A grants, B alone denies, mixin-only alone denies, B never inherits TrustGroup). No schema or migration.
- `OnePathAuthorizationTest` / `DistributiveLawTest` / `CompilerIsolationBackendTest` / `CoordinatorQueryCountTest` / `ObjNoneBoundaryTest` / `UndeclaredJunctionRemainsHistoricalTest` / `CompilerFailureTest` / `RegisteredOrdinaryModelTest` / `HistoricalFallbackCapabilityTest` / `MixedPathUndeclaredJunctionTest` / `QuerySetCallableConditionTest` — issue #77 S3b: declared Category/Ticket/Trust `has_perm` / `has_perms` / `get_all_permissions` / `get_group_permissions` on the path-scoped compiler (trustee and historical group-only; wrong perm / other Trust deny; conditions overlay; object and QuerySet parity); registered ordinary non-`Content` instance/QuerySet/enum/`.permitted`; distributive-law A-grants-C1 / B-grants-C2 (Django/object grant each row, aggregate QuerySet `has_perm` true, common enumeration contains P, `.permitted` both in 1 SQL); failed ceiling cannot borrow another path's fragment; concrete group route grants and mixin-only plan route denies; mixin-only with no plan denies TUP/TrustGroup while concrete historical_fallback still authorizes undeclared Junction/Group and an inapplicable mixin neither adds nor suppresses that fallback; coordinator vs non-coordinator query counts including reversed order and duplicate exact path strings; `get_group_permissions` is group-only; Trusts-only `obj is None` is false/empty and `ModelBackend` + Trusts restores globals without object grants; QuerySet plus callable raises `PermissionConditionNotQueryable` with zero callbacks; malformed/raising/unconfigured/ambiguous compilers fail loud. No schema or migration.
- `AnyPlanRecordsGateRuleTest` / `FilterByUserContentPermRegistryGateTest` / `MultiPathCreateUnderTrustGateTest` / `NewTeamFormTrustChangeTest` — issue #80 S4: `filter_by_user_content_perm` support gate is `any_plan_records` (any configured path with `plan_for(content).records`); grant stays `trust_grant_q` on Trust rows. Registered Category / Ticket / Trust / proxy / instance stay lazy, distinct, one SQL; correct permission grants; wrong permission / wrong Trust / inactive / anonymous / excluded root deny; `:condition` raises before the gate; unknown/unregistered (Organization, User, Junction/Group, emptied Category despite `_contents` + `historical_fallback`) return `none()` without a grant query; multi-path declaration supports the terminal without compiler / `filter_authorized` leakage; `NewTeamForm` Trust/`change` excludes root. No schema or migration.
- `TrustsRegistryDependentGrammarRegistrationTest` / `TrustsRegistryJ1ProjectionTest` / `TrustsRegistryD1ProjectionTest` / `TrustsRegistryD1oProjectionTest` / `TrustsRegistryD2ProjectionTest` / `TrustsRegistryD2oProjectionTest` / `TrustsRegistryD2fProjectionTest` / `TrustsRegistryDependentGrammarGroupSplitTest` / `TrustsRegistryDependentGrammarToFieldTest` — issue #83 S5: bounded generic `Ref` content grammar accepts J1 (F+rev O2M+F), D1 (gateway+rev O2M), D1o (gateway+rev O2O), and D2/D2o/D2f (two suffix hops: rev O2M, rev O2O, parent-held forward). Isolated mock models only. Existing direct/trailing-reverse registrations stay valid. Rejects reverse-before-gateway, reverse-only, reverse O2O gateway, M2M, GFK, suffix depth > 2, all-forward multi-hop, composite targets, mixed roots, #57/#65 conflicts. Shared-row correlation, wrong user/permission/content denial, sibling/mixed-scope isolation, fail-closed malformed paths. Object auth, aggregate `all_match`, and `common_permissions` agree for each mock terminal. Direct vs group-only contribution is compiler-split with no Python per-object loop. Each grant/all-match/enumeration projection stays one SQL independent of candidate count. No schema or migration.
- `GroupContributionIdempotenceTest` / `IsolatedAppsDoesNotDonateGroupContributionTest` / `GroupContributionPathTest` / `GroupAuthorizationRegistryTest` / `GroupCompilerIsolationTest` / `GroupPlanUsesContentExistsTest` — issue #85 S6: host AppConfig contributes TUP→Trust←Junction→Group via `_meta` / Junction contract (J1, full-path correlated, independent of `Content._contents`); contributor-instance + exact-registry sentinel (re-enter no-op; Category/Ticket sentinels do not complete Group; different-root Group still registers TUP; conflict fails closed without Group sentinel; new/swapped registry receives the declaration again; omitted/ambiguous/unconfigured path fails before writing; `isolate_apps`); Group instance and QuerySet `has_perm` / `has_perms` / `get_all_permissions` / `get_group_permissions` on the registered plan (direct trustee and TrustGroup; mixed-Trust all-match deny; direct vs group-only split; wrong user/perm/sibling/inactive/anonymous/`obj is None` deny); Group no longer reaches `_get_trusts` / `filter_by_content`; mixin-only isolation (no plan denies; plan-only gets trustee not TrustGroup; concrete keeps both; inapplicable mixin neither adds nor suppresses); one-SQL grant/all-match/enumeration independent of candidate count; Category/Ticket/Trust unchanged; `filter_by_user_content_perm` treats Group as a known terminal. No schema or migration.
- `LegacyContentRegistryDeletedTest` / `ConditionRegistryPreservedTest` / `HistoricalCompilerPreservedTest` / `LiveTerminalParityTest` / `DependentHostContributionTest` / `IsolatedAppsDoesNotDonateDependentContributionTest` / `DependentAuthorizationTest` / `UnknownTerminalFailsClosedTest` — issue #87 S7: static `Content._contents`, `is_content*`, `get_content_fieldlookup`, `filter_by_content`, `_get_trusts`, and the content-map half of `class_prepared` / `register_junction` are gone (runtime/behavioral structure, not source-token regex); `register_content` / `register_junction` walk Meta conditions only; Ticket `meta_own`, Trust `own`, Junction content conditions, duplicate/error handling, and system checks stay exact; `HistoricalGroupQueryCompiler` remains and mixin-only isolation is unchanged; Category/Ticket/Trust/Group allow/deny, direct/group-only, mixed-Trust, inactive/anonymous, `obj=None` unchanged; isolated host AppConfig `Ref` contributions for both documented dependent levels (ReceiptImage / ReceiptImageMeta equivalents) cover object `has_perm`, aggregate QS all-match, `common_permissions`, mixed-scope denial, and direct vs group-only; each grant/all-match/enumeration projection is one SQL independent of candidate count; unknown and undeclared manual dependents fail closed and never reach a deleted fallback. No schema or migration.
- `AlongRegistrationTest` / `AlongIdentityRejectionTest` / `AlongShapeAndGraphTest` / `AlongToFieldAndUuidTest` / `AlongRuntimeGateTest` / `AlongE005Test` — issue #92: isolated noun-neutral models only. S/C/E metadata; terminal = walk-site; reverse-O2M, two-level reverse, reverse O2O, and forward multi-valued-inverse suffixes; non-PK `to_field`; integer/text/UUID identities; every rejected identity family plus mixed/mismatched/UUID-vs-text; frozen `register` first; zero mutation and zero SQL on registration failure; self-link, two-node cycle, uneven diamond, duplicate grants/paths, NULL/dangling, empty seed, bound cut; direct + recursive `OR` and multiple handles; shared-row; all three projections plus `all_match` at one SQL each; queryset filter before slicing; `EXPLAIN QUERY PLAN` materializes the generation walk once; actual-alias runtime gating; E005 with no aliases / supported / unsupported / failed probe / several aliases; isolated registry unscanned; non-recursive SQL and results unchanged. No schema or migration.
- `ClosedPredicateRegistrationTest` / `ClosedPredicateAuthorizationTest` / `ClosedPredicateEqualToFieldAuthorizationTest` — issue #98: isolated GH-shaped models. Public `All` / `Equal` / `permission_in` plus the accepted team spelling (`user=t.team.members`, `condition=All(permission_in(...), Equal(...))`) register with **0 SQL**. Untyped / empty / mixed-root / incompatible / extra-multi-valued declarations fail closed without mutation. `Equal` paths that share a model but not one resolved comparison field (`to_field` slug vs code) fail closed with **0 SQL**; matching `to_field` registers and same-object allows / split objects deny at **1 SQL**. Team member + grant + bundle operation + same organization allows; removing membership, grant, bundle operation, or organization alignment denies only that branch; cross-org team grants deny. Direct and team roots OR; revoking one valid root preserves the other. Object, queryset/manager, and enumeration agree at **1 SQL**. Query construction is lazy; listings filter before pagination. Reverse O2M requester paths stay rejected. No schema or migration.
- `ConstructTimeSelfBindTest` / `ImportOrderStandaloneConstructionTest` / `LiveEnsureSelfBindTest` / `OverrideUnbindAndFailClosedTest` / `NamedConditionCompilationTest` — issue #151 C1: `TrustsRegistry()` self-binds the private store adapter (`lookup.conditions is registry.conditions`); `freeze()` / `ready()` do not bind; live `_ensure` registries are bound without host `_ir` wiring; isolated subprocess import-order proofs (`trusts.core` then construct does not preload `_ir`; `_ir` then construct is not circular; `trusts.apps` registers `Meta.permission_conditions` without loading `trusts.core` / `_ir`); explicit unbind and partial-bind non-mutation stay; unknown / unbound-record / malformed fail closed; construction / registration / `freeze()` are zero SQL; Core-only named-condition compilation through `record_for` / `compile_q` / `compile_registered_condition_q` / `evaluate_permission_condition`. Six-name `trusts.conditions` surface unchanged. No schema or migration.
- `KernelConfigTest` / `LegacyMatrixATest` / `ConditionLookupBindTest` / `ConditionLookupBackendAdapterTest` / `AuthorizedQuerySetSurfaceTest` / `AuthorizedQuerySetLiveTest` / `FilterAuthorizedScopesIsolatedTest` / `FilterAuthorizedScopesToFieldTest` / `FilterAuthorizedScopesLiveTest` — historical issue #54 coverage (superseded by the later core/Zero split): `kernel_config()` then resolved `get_app_config('trusts')` by class identity; matrix A still populates `label='trusts'` with `trusts.0001_initial` / `0002_trustgroup` from `trusts.migrations` and no Zero package; `ConditionLookup` bind is zero-SQL, missing methods do not partial-bind, callables are never invoked; unbound overlay preserves `Content` conditions; `AuthorizedQuerySet.authorized` rejects strings with zero SQL, does not call `is_active_principal` / `get_permission`, AND-overlays `extra_q`, and has no `.permitted` / `.get_permission`; `filter_authorized_scopes` fail-closes unknown prefix / empty handles / content-terminal queryset / wrong permission type with zero SQL, correlates prefix hops to the resolved target field (non-PK `to_field` slug identity, sibling deny, lazy, one SQL), and does not use historical TrustGroup (live `filter_by_user_content_perm` still does). No schema, label, or migration change. `trust_grant_q` / `HistoricalGroupQueryCompiler` remain.

Do not treat a gap as license to widen access. New grants need an explicit
test.
